import Button from "react-bootstrap/esm/Button";

const VisitorButton = (params) => {
    return (
        <>
            <Button>
                <h1>Visitor Button</h1>
            </Button>
        </>
    );
};

export default VisitorButton;
